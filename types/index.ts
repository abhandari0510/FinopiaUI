export type Locale = "en" | "mr";

export type Service = {
  icon: "chart" | "shield" | "heart" | "book" | "users" | "family" | "graduation" | "sunset" | "discipline";
  title: string;
  description: string;
};
